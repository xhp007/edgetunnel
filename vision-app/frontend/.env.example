# Base URL for the FastAPI server (leave empty to use the same origin / Vite proxy)
VITE_API_BASE_URL=

# Optional override for the Vite dev server proxy target
VITE_PROXY_TARGET=http://127.0.0.1:8000
